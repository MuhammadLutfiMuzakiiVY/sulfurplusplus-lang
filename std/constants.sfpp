export this as std/constants;

expose "SECOND" as SECOND;
expose "MINUTE" as MINUTE;
expose "HOUR" as HOUR;
expose "DAY" as DAY;
expose "WEEK" as WEEK;

expose "RUNTIME_VERSION" as RUNTIME_VERSION;
expose "SULFUR_VERSION" as SULFUR_VERSION;
expose "COMBUST_VERSION" as COMBUST_VERSION;
expose "BUILD_MODE" as BUILD_MODE;
expose "DEBUG_MODE" as DEBUG_MODE;

expose "OBJ_NAME" as OBJ_NAME;
expose "GET_ITEM" as GET_ITEM;
expose "PROPS" as PROPS;