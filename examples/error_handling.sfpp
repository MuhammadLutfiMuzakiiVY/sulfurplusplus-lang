// error_handling.sfpp
// Demonstrates error handling keywords

fn fragileFunction() {
    // Defer a block to run at the end of the function regardless of outcome
    defer {
        // Close resources here
    }
    
    try {
        var x = 10 / 0; // Might throw error
    } catch err {
        // Handle error
    }
}
