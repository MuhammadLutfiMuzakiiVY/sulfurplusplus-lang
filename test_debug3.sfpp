import std/http as http;
import std/io as io;

io.Terminal.Out << "Testing http.get..." << io.Terminal.EOL;
var resp = http.get("https://httpbin.org/get");
io.Terminal.Out << "resp type: " << typeOf(resp) << io.Terminal.EOL;
